Place the Windows kiro-rs sidecar executable here as `kiro-rs.exe`.

The binary is bundled separately (gitignored) — this placeholder keeps the
`resources/kiro/**/*` bundle glob non-empty so `tauri build` succeeds in CI.
